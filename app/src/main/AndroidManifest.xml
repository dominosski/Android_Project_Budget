<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mybank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.AddNoteActivity"/>
        <activity android:name=".Activities.AddProjectActivity" />
        <activity android:name=".Activities.NotesActivity" />
        <activity android:name=".Activities.ProjectActivity" />
        <activity android:name=".Activities.StatsActivity" />
        <activity
            android:name=".Activities.TransactionActivity"
            android:windowSoftInputMode="adjustPan|adjustResize" />
        <activity android:name=".Activities.LeasingActivity" />
        <activity android:name=".Activities.AddLeasingActivity" />
        <activity android:name=".Activities.TransferActivity" />
        <activity android:name=".Activities.OutgoingActivity" />
        <activity android:name=".Authentication.LoginActivity" />
        <activity android:name=".Authentication.RegisterActivity" />
        <activity
            android:name=".Activities.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>



