<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mybank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddLoanActivity"></activity>
        <activity android:name=".TransferActivity" />
        <activity android:name=".InvestmentActivity" />
        <activity android:name=".AddInvestmentActivity" />
        <activity android:name=".Models.ShoppingActivity" />
        <activity android:name=".Authentication.LoginActivity" />
        <activity android:name=".Authentication.RegisterActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>